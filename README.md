- create a `.env` file inside backend folder and put variable values as told inside `.env.example`
- run `npm run install` to install libraries
then use commands

`build`: to build the app

`start`: to start the server